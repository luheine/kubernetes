image:
  repository: host.docker.internal/floorball
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8080

ingress:
  enabled: true
  hosts:
    - floorball.lamp.local
  tls:
    - hosts:
        - floorball.lamp.local
      secretName: floorball-tls

