# homelab-minikube-gitops (generated)

This repo was generated by setup_full_homelab_repo.sh

## Quickstart (WSL Ubuntu)

1. Start Minikube
minikube start --driver=podman --memory=8192 --cpus=4

2. Run Ansible to setup the cluster components:
ansible-playbook playbooks/setup_cluster.yml

3. Apply the root ArgoCD app (if not already applied by playbook):
kubectl apply -f clusters/minikube/apps/root-application.yaml -n argocd


4. Optional: update /etc/hosts to point *.minikube.local to minikube IP.

