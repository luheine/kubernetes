---
- hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Ensure minikube running
      shell: minikube status | grep "host: Running" || minikube start --driver=podman
      changed_when: false

    - name: Run setup_cluster
      import_playbook: setup_cluster.yml
